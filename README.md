# CodePlay-GPU

OS - LINUX (WSL2: UBUNTU)

GPU - GEFORCE RTX 3060

CUDA - 12.6 (any GPU with cuda 12.6 will work)

ffmpeg - sudo apt-get install ffmpeg

rubberband-cli - sudo apt-get install rubberband-cli
